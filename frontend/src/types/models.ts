export type Subscription = {
  _id: string;
  quantity: string;
  frequency: string;
  status: string;
};

export type Delivery = {
  _id: string;
  date: string;
  quantity: string;
  status: string;
};
